[project]
name = "deepseek-function-calling"
version = "0.1.0"
description = "DeepSeek工具调用示例"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
    "openai>=1.0.0",
    "jupyter>=1.0.0",
]
